<?php

declare(strict_types=1);

namespace Trap;

/**
 * A real class the completion sources would legitimately offer once the walker
 * reads this directory as the `Trap\` PSR-4 root: its filename and namespace
 * match, so `Trap\RealType` is a valid candidate here.
 */
class RealType
{
}

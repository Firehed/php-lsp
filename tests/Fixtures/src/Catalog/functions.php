<?php

declare(strict_types=1);

namespace Fixtures\Catalog;

/**
 * A helpers file: functions, no class. A PSR-4 directory listing reports it as a
 * coarse class-like (`Fixtures\Catalog\functions`) without parsing it, so it is
 * the phantom the existence gate must drop from navigation.
 */
function noop(): void
{
}

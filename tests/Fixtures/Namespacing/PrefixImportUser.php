<?php

declare(strict_types=1);

namespace App\Model;

/**
 * A class whose fully-qualified name is also a namespace prefix: `App\Model\User`
 * is this class, and `App\Model\User\Repository` lives beneath it. An import of
 * this class therefore contributes both `User` and its qualified children.
 */
class User
{
}

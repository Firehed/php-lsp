# Build manifest

One goal: no positional or knowledge question has more than one implementation, and completion offers every symbol kind the knowledge tier answers.
One signal: both baseline files are deleted, and the issues named below are closed.

## Rules

- Until every box below is ticked, this list is the only work. Nothing outside it starts unless the human schedules it directly.
- One step is one PR on branch `step/<n>`.
- Steps run in order. Step n starts when step n-1 has merged.
- The PR for a step ticks its own box. A ticked box means merged.
- The rule set is closed: no new PHPStan rule, deny entry, deptrac edge, allowlist path, baseline entry, `@phpstan-ignore`, or RFC amendment, except a Tighten or Lateral edit that the step's row names (that naming is the human's authorisation under `enforcement-edits.md`). Removing any of those is always allowed. A gap found on the way becomes a GitHub issue.
- A step's `Done` clause is its whole acceptance. A reviewer checks that clause and nothing else.
- Background reading is RFC 1 §4 (the invariants). Plan 0002 is history, not work.
- One route per fact. Where two routes to one fact exist (a tree and a regex, two stores, a native type and a docblock), they meet in exactly one class, which hands every consumer one shape. A consumer never holds two routes and never learns which one answered. `OneRoutePerFactTest` (step-32) lists each fact's ingredients and its one holder; a step that names an ingredient outside its holder, or adds a skipped row, is not done.
- A skipped test case runs its assertion first and skips only when the assertion fails; a listed case whose assertion passes fails the test, so the entry leaves with the fix. A skip that runs before its assertion hides a green cell and is a finding.
- A test that tells states or routes apart by fixture pins each state's defining property with its own assertion, and its fixtures differ only in that property. A fixture chosen because it exists, rather than because it fits the row's own words, is a finding.
- A row states facts about the code. If a fact the row states is false, or a thing it names does not fit the row's own words, the implementer stops and reports; choosing an interpretation is not an option.
- Regex stays in the files `phpstan.neon` allows it in.
- When step-50 lands, this file and its two driver skills are gone, and work returns to plain issue flow: pick an issue, "do #xxx".

## Steps

- [x] **step-0** — Feature-matrix grid, the net for the rewrite behind it: rows are fixture scenarios (file plus cursor marker), columns are the handlers, derived from the handler registry. Each cell asserts that handler's observable or is registered not-applicable against an issue number; an unregistered cell fails, and so does a registration on a cell that answers. Done: the grid runs in `composer test`; adding a handler adds a column with no edit to the test; #445 closed.
- [x] **step-1** — `SymbolSource::search(prefix, NameKind)` replaces `searchClassLikes` on `SymbolSource` and `SymbolBackend`; `OpenDocumentBackend` answers for every kind; `ClassCandidates` passes `NameKind::ClassLike`. Done: `searchClassLikes` is gone; every parity golden is unchanged.
- [x] **step-2** — `FilesystemBackend` answers `search` for functions and constants from the autoload.files index; `BuiltinBackend` answers it from reflection enumeration. Done: `SymbolCoverageGridTest` registers no `search` cell for `Function_` or `Constant`.
- [x] **step-3** — `ReflectionSymbolInfoFactory::classInfo()` answers only where `isInternal()` is true. Done: #429 closed.
- [x] **step-4** — Delete `FunctionRepository` and `DefaultFunctionRepository`. `SymbolResolver` and `BasicTypeResolver` resolve a function name as `NameContext` candidates through `SymbolSource::lookupFunction`. Delete `CodeResolver::getFileFunctions`. Remove the `FunctionRepository` exemption from `SymbolDiscoveryAuthorityExtension`. Done: `src/Repository/*FunctionRepository.php` is gone; its baseline entries are gone.
- [x] **step-5** — One `SymbolCandidates` replaces `ClassCandidates` and `FunctionCandidates`. For the kinds a position allows it calls `search`, adds imports from all three tables and `childrenOf(current namespace)`, resolves each through `ReferenceResolver`, applies the position predicate, and builds every item with one `CompletionItemFactory::forSymbol()`. `NamespaceCandidates::offerSymbol` applies the same filter and no longer drops non-class-likes. `ClassCandidates::indexKinds` is gone. Done: #317, #239, #383 closed; the only `src/Completion/` baseline entries left are those in `MemberCandidates`; a test asserts every kind `search` answers is offered at expression start.
- [x] **step-6** — The argument-value position offers what expression start offers, plus named arguments. Done: #292 closed.
- [x] **step-7** — `MemberCandidates` has no branch on `MemberAccessKind` and no `instanceof`; which members an access kind admits is decided in `MemberAccessContext` and `MemberFilter`. Done: `MemberCandidates` has no baseline entry.
- [x] **step-8** — Trait completion after `use` in a class body: one `isTrait` predicate, one filter case, one classifier case. Done: #316 closed; the diff adds no source class.
- [x] **step-9** — One node walk: `NodeAtPosition::find(ast, offset, filter)`, moved to `src/Utility/`. `Scope::findEnclosingFunctionLike` and `SymbolResolver::findCallAtPosition` call it instead of walking. Done: `SymbolResolver` has no traversal baseline entry; `Scope` and `ScopeFinder` are removed from the traversal allowlists.
- [x] **step-10** — One enclosing class-like: `Scope::atOffset` carries the enclosing class-like node for all four kinds, and one function in `src/Resolution/` answers the question, reading `Scope` when the AST is non-empty and `TextFallbackHelper`'s regex when it is empty. Delete `SymbolResolver::findEnclosingClassForLine` and `ScopeFinder::findClassAtLine`. Done: the skipped cases in `AstTextAgreementTest` run and pass.
- [x] **step-11** — One name context: `NameContextFactory` reads `TextFallbackHelper`'s regex only when the AST holds no namespace or `use` node, and `NameContext::candidates(short, NameKind)` is the one place a short name becomes FQN candidates. Delete `SymbolResolver::resolveNameFromText`, `TextFallbackHelper::resolveClassName`, `resolveFromUseStatementsText`, and the import map `extractUseStatementsFromText` builds outside the factory. Done: those methods are gone; `SymbolResolver` has no `preg_match` baseline entry; `AstTextAgreementTest` covers namespace and imports.
- [x] **step-12** — `CallContextDetector` in `src/Resolution/`: the AST path and the text path of `getCallContext` in one class, using the walk from step 9 and the names from step 11. Done: `findCallAtPosition`, `findCallFromText`, `parseCallPattern`, `parseArgsFromText` are gone from `SymbolResolver`; `AstTextAgreementTest` covers call context.
- [x] **step-13** — `MemberAccessDetector` in `src/Resolution/`: the AST path and the text path of `getMemberAccessContext` in one class, with one function that decides visibility from the vantage class and the target class. Delete the four visibility copies and `TextFallbackHelper`'s member-access and parameter-type methods. Done: one visibility function; `AstTextAgreementTest` covers member access.
- [x] **step-14** — One `ExpressionResolver` returns a `ResolvedSymbol` for any expression; its type is `->getType()`. One `VariableBindings::before(scope, offset)` lists parameters, assignments, `foreach`, `catch`, and closure `use` bindings with their nodes; `getVariablesInScope`, variable typing, and `ResolvedVariable::getDefinitionLocation()` read it. Delete `src/TypeInference/`, `ExpressionTypeResolver`, and `TextFallbackHelper::resolveChainType`. Done: `src/TypeInference/` is gone; #301 closed; a `foreach` variable resolves members.
- [x] **step-15** — The `*Info` objects implement `ResolvedSymbol` and `ResolvedMember`; the `Resolved*` wrapper classes are deleted; `ResolvedSymbol` and `Location` move to `Domain`. Done: #416 closed; `SymbolResolver::getMembersForClass` is one loop.
- [x] **step-16** — Unify member lookup on broken files at the `ClassInfo` seam. A text-based `ClassInfo` producer (moved out of `TextFallbackHelper::extractMembers`) sits alongside the AST- and reflection-based producers; `DocumentSymbolSink::write` selects, cheapest first: AST when the parse yields declarations, preserved registration when the parse is empty and a prior one exists, text producer when neither. `SymbolResolver::getAccessibleMembers` loses its fallback branch — `MemberResolver` answers uniformly. `extractMembers`' inheritance walk is deleted; `MemberResolver::supertypes()` walks the text-derived `ClassInfo` the same way it walks any other. Done: `SymbolResolver::getAccessibleMembers` has no branch; `extractMembers` and its inheritance walk are gone; a `ClassInfoFactory` produces the text case; `DocumentSymbolSinkTest` pins the three-tier producer selection; the broken-file completion parity goldens (`tests/Parity/goldens/completion-broken-*.json`) stay byte-for-byte green.
- [x] **step-17** — Delete `MemberAccessDetector::walkChain` and `TextFallbackHelper::splitChainParts`. Multi-step chain typing on broken code reads the last-good AST that step-16 preserves, so a chain receiver walks through `ExpressionResolver::resolve` rather than a parallel string walker. Done: neither method exists; `ExpressionResolver` is the only chain-type walker; `CompletionHandlerTest`'s chain-completion cases still pass; the `MemberAccessDetector.php` baseline entry, if any remained, is gone; `ExpressionResolver::resolveMethodCall` reaches 100% coverage through the chain-typing exercised by the completion, hover, and definition suites; `MemberAccessDetector` and `TextFallbackHelper` reach 100% coverage via the two deletions.
- [x] **step-18** — Delete the `$this` branch in `SymbolResolver::resolveVariable`; the `$this` case routes through `ExpressionResolver::resolve` so the `Variable('this')` → type answer lives in exactly one place. Done: `SymbolResolver::resolveVariable` has no `$name === 'this'` special case; hover, definition, and completion on `$this` still answer; `ExpressionResolver::resolve`'s `Variable('this')` branch reaches 100% coverage through those same suites; `SymbolResolver` reaches 100% coverage via the deletion of the `$this`-outside-class branch.
- [x] **step-19** — Delete every remaining `SymbolResolver::resolve*` node dispatcher — `resolvePropertyFetch`, `resolveStaticPropertyFetch`, `resolveClassConstFetch`, `resolveConstFetch`, `resolveFunctionCall`/`resolveFunctionByName`, `resolveStaticCallCallable`, `resolveNewCallable` — routing each node kind through `ExpressionResolver::resolve` (or its per-kind method) instead. `MemberAccessDetector::resolveInstanceAccessType` remains the receiver-type entry (it already routes through `ExpressionResolver::resolve`) but is no longer consumed by `SymbolResolver` for any of these kinds. Done: `SymbolResolver` has none of the listed methods; every `PropertyFetch`, `NullsafePropertyFetch`, `StaticPropertyFetch`, `ClassConstFetch`, `ConstFetch`, `FuncCall`, `StaticCall`, and `New_` answer comes from `ExpressionResolver`; `ExpressionResolver` reaches 100% coverage through the existing hover, definition, signature-help, and completion suites.
- [x] **step-20** — Both `ClassInfo` factories add the implicit `UnitEnum` and `BackedEnum` interfaces; the trait edge carries `insteadof` and `as` adaptations and `MemberResolver` applies them. Done: #423 and #73 closed; `TypeGraphParityTest` has no skipped case.
- [x] **step-21** — Route the remaining `new ClassName`, `new PrimitiveType`, and `new UnionType` sites through `TypeFactory`. Delete `phpstan-baseline.neon` and `deptrac.baseline.yaml`; `bin/check-baseline-shrink` fails if either file exists (this edit to `bin/` is authorised here). Done: no baseline file exists; CI is green; #181 closed.
- [x] **step-22** — `SymbolCandidates` absorbs `NamespaceCandidates`'s namespace-tree navigation, so every position that offers symbols calls one source. When the prefix is namespace-qualified or `\`-rooted, `SymbolCandidates` navigates the tree internally; otherwise it does flat lookup. `CompletionHandler` has one symbol-completion call per position instead of parallel `SymbolCandidates` + `NamespaceCandidates` calls. Done: `NamespaceCandidates` is deleted; `getClassCompletions` and `getExpressionCompletions` each call `SymbolCandidates` once; `\`-prefixed function and constant completion works at expression start; filtered positions (`implements`, `extends`, trait `use`, `catch`, `#[…]`) no longer offer functions or constants via namespace navigation.
- [x] **step-23** — `SymbolResolver::resolveCallable` answers every callable-shaped node (`FuncCall`, `MethodCall`, `NullsafeMethodCall`, `StaticCall`, `New_`, `Attribute`) by delegating to `ExpressionResolver`: `FuncCall`, `MethodCall`, `NullsafeMethodCall`, and `StaticCall` go through one `ExpressionResolver::resolve` call, and `New_` and `Attribute` go through one `ExpressionResolver::resolveConstructor` call (the constructor question is separate from the type question `resolve(New_)` answers). The method-call path applies late-bound return-type resolution the same way the static-call path does. Done: `resolveCallable` has no `match`/`switch`/`instanceof` on the call-node kind and no direct `MemberResolver::findMethod` call; hover on `$obj->foo()` where `foo(): static` reports the receiver's class the same way hover on `Foo::bar()` does; a parity test asserts hover-signature agreement across all callable node kinds for `self`/`static`/`parent` return types.
- [x] **step-24** — Member lookup in `ExpressionResolver` is one function taking the receiver expression, the member name, and a kind-specific finder; `resolveMethodCall`, `resolveStaticCall`, `resolvePropertyFetch`, and `resolveStaticPropertyFetch` call it. Done: the four methods share one member-lookup helper; adding a fifth member-access node kind is one call site, not four; the existing hover, definition, completion, and signature-help suites remain green.
- [x] **step-25** — `ExpressionResolver::docblockForExpression` reads the resolved symbol's docblock through one `resolve(...)?->getDocumentation()` call — no per-kind branch. If the wrapper carries no logic once the branch is gone, delete it and inline the call at every caller. Done: the method either does not exist or is one line with no `match`/`instanceof` on the expression node; `@return list<T>` and `@var` docblock inference works on `FuncCall`, `MethodCall`, `NullsafeMethodCall`, `StaticCall`, `PropertyFetch`, `NullsafePropertyFetch`, `StaticPropertyFetch`, `ClassConstFetch`, and `ConstFetch` the same way it works on `$this->items()`; a test covers each of those node kinds.
- [x] **step-26** — The three late-binding keywords (`self`, `static`, `parent`) resolve in one place: `Domain\LateBindingKeyword`. Every reader (`ScopeFinder`, `MemberAccessDetector`'s text and AST paths, any other) identifies a keyword through `LateBindingKeyword::tryFrom(strtolower($name))` and resolves it through one function on the enum; the `parent`-of-non-`Class_` guard exists there once. A test under `tests/Architecture/` fails if a string comparison against `'self'`, `'static'`, or `'parent'` appears in `src/` outside `src/Domain/LateBindingKeyword.php` — the tighten that pins the seam. Done: no `src/` file outside the enum compares against the three keyword literals in a class-name-resolution context; a text-path and an AST-path test exercise the same behavior through one code path; the architecture test above is green.
- [x] **step-27** — `ExpressionResolver::resolveMember` (introduced in step-24) iterates every class it gets from the receiver's `Type::getResolvableClassNames()` instead of indexing `[0]`, the same way `SymbolResolver::getAccessibleMembers` iterates. `MemberAccessDetector`'s three instance-receiver sites route through the same helper (or apply the same iteration). Tighten: `disallowedMethodCalls` restricts `Type::getResolvableClassNames()` to the shared helper and to `SymbolResolver::getAccessibleMembers`, so a future direct caller fails PHPStan. Done: no callsite in `src/Resolution/` indexes `[0]` on `getResolvableClassNames()`; hover, definition, and signature-help on `$x->onlyB()` where `$x: A|B` and only `B` declares `onlyB` answer the same way completion offers it; a parity test asserts the four positional handlers and completion agree on union and intersection receivers; the phpstan baseline for the rule reaches zero.
- [x] **step-28** — `resolveConstFetch` iterates `NameContext::candidates(short, NameKind::Constant)` the way `resolveFuncCall` iterates `NameKind::Function_`, so PHP name-resolution rules 5-7 (namespaced-first, global fallback) apply to constants as they do to functions. Tighten: `disallowedMethodCalls` restricts `SymbolSource::lookupConstant` to `src/Resolution/ExpressionResolver.php` (mirroring the #478 pattern for `findMethod`/`findProperty`), so a future direct `lookupConstant` outside the candidate loop fails PHPStan. Done: hover and definition on `X` in `namespace App; const X = 1; echo X;` answer; hover and definition on `PHP_INT_MAX` in a namespaced file with no `use const` answer; `resolveConstFetch` has no direct `lookupConstant` call that bypasses the candidate loop; a test covers both the namespaced-constant and global-fallback paths.
- [x] **step-29** — `SymbolCandidates` reads a symbol's documentation through the `ResolvedSymbol::getDocumentation()` interface method, not by direct `->docblock` field access plus `DocblockParser::extractDescription`. Tighten: `disallowedMethodCalls` restricts `DocblockParser::extractDescription` to `src/Domain/HasSymbolLocation.php`, so a second bypass of the interface fails PHPStan. Done: `SymbolCandidates` does not name `->docblock` or `DocblockParser` directly; a future change to `getDocumentation()` (e.g. tag stripping) reaches completion detail the same way it reaches hover.
- [x] **step-30** — Signature-plus-documentation assembly lives in one place. A `ResolvedSymbolPresenter` in `src/Resolution/` returns the shape (signature, documentation, deprecation, tags) that `HoverHandler`, `SignatureHelpHandler`, and `CompletionItemFactory` all consume. Each handler and factory maps the presenter output into its LSP shape but does not compose signature-and-documentation itself. The presenter also owns docblock text preparation: tags stripped so every surface shows a description rather than the raw docblock, restoring the completion detail that step-29 temporarily regressed. The extraction call (`DocblockParser::extractDescription`, whose step-29 allowlist named `src/Domain/HasSymbolLocation.php` as its home) either moves here or the allowlist grows to include the presenter's path — a Lateral edit either way, since `getDocumentation()` itself must keep returning the raw docblock for `ExpressionResolver`'s foreach element-type inference. Tighten: `disallowedMethodCalls` restricts `ResolvedSymbol::format()` and `ResolvedSymbol::getDocumentation()` to `ResolvedSymbolPresenter`, so a future handler that recomposes signature-plus-doc directly fails PHPStan. Done: no handler or factory calls both `->format()` and `->getDocumentation()` on a resolved symbol; adding a new user-facing field to `ResolvedSymbol` (e.g. `getDeprecation()`) is one edit that all three surfaces read; a parity test asserts hover, signature-help, and completion-detail surface the added attribute the same way; the function-surface parity golden's `documentation` fields carry extracted descriptions, not raw docblocks; the disallow above is in place.
- [x] **step-31** — `Variable('this')` types through one code path. `ExpressionResolver::resolve(Variable('this'))` reads the enclosing class the same way `MemberAccessDetector` reads it — a shared helper that consults the text fallback when the parent chain is detached — and the `resolvedType` attribute side-channel `MemberAccessDetector` sets on the `$this` node is deleted. Tighten: `disallowedMethodCalls` restricts `TextFallbackHelper::resolveEnclosingClassName` to the shared helper, so a future ad-hoc text-fallback caller fails PHPStan. Done: hover on `$this` and completion on `$this->` answer for the same set of broken-code inputs; a fixture whose enclosing class-like parent chain is detached drives both features through one path; `MemberAccessDetector` does not write `resolvedType` on any AST node; the disallow above is in place.
- [ ] **step-32** — The one-route ledger. `tests/Architecture/OneRoutePerFactTest.php` holds a registry: one row per fact, each naming the fact, its ingredient classes (the routes), the one class that holds them, and any assembly root (`Server::forProject`, `KnowledgeStack::forProject`) that may name them to wire the holder. The test parses every file under `src/` and fails when a file outside the row's holder and roots names an ingredient class in any form (type hint, `new`, static call, `instanceof`, `::class`); a canary under `tests/Architecture/data/` drives every form. A row names only classes that exist, so a row whose ingredient or holder is not yet written names today's equivalent and the step that swaps it. A pending case runs its assertion first: it asserts the violators are exactly the recorded set, then skips naming their steps, so a violator that appears or disappears fails the row. Initial rows: syntax tree production (ingredients `PhpParser\Parser` and `PhpParser\ParserFactory`, holder `ParserService`; step-35 swaps the ingredients for the producer classes); cursor syntax (ingredient `TextFallbackHelper`, no holder because the class is deleted at the end; pending `DefaultTextSymbolExtractor` step-37, `MemberAccessDetector` step-39, `CallContextDetector` step-40, `SymbolResolver`, `EnclosingClassResolver`, `NameContextFactory` step-41); symbol lookup (ingredients `OpenDocumentBackend`, `FilesystemBackend`, `BuiltinBackend`, holders `CompositeSymbolSource` for reads and `DocumentSymbolSink` for writes, root `KnowledgeStack`; nothing pending); open-document declarations (ingredient `SymbolIndex`, holder `OpenDocumentBackend`, root `KnowledgeStack`; pending `WorkspaceNamespaceSource`, `DocumentIndexer`, `DocumentSymbolSink` step-45); name resolution rules (ingredients `PhpParser\NameContext` and `PhpParser\NodeVisitor\NameResolver`, holders `ParserService` and `Resolution\NameContext`; nothing pending, because the ledger sees class names and not hand-written rules — steps 43 and 44 close that fact); docblock types (ingredient `DocblockParser`, holders `TypeFactory` for types and `ResolvedSymbolPresenter` for descriptions; pending `ExpressionResolver` step-48). A row with zero ingredients, or naming a class that does not exist, fails. Done: the test runs in `composer test`; the canary test proves every reference form is caught; every skip names a step in this file; `CLAUDE.md` Architecture Invariants gains "One route per fact" naming this test as its mechanism, and the Guardrails self-check list names it.
- [ ] **step-33** — The parse-health grid. `tests/Resolution/ParseHealthGridTest.php` runs every position-taking `CodeResolver` method (derived from the interface by reflection, so a new method is a new row with no edit to the test) against one scenario in three parse states: clean (php-parser reports no error), recovered (php-parser reports an error but yields statements), and empty (php-parser yields no statements). The three fixtures under `tests/Fixtures/src/ParseHealth/` are the same class with the same `strlen($this->` at the cursor and differ only in how the text after the cursor is broken; a precondition test per state asserts that state's parse outcome with php-parser directly, so a fixture edit cannot merge two columns. Each cell asserts one observable appropriate to its method — the enclosing namespace, the enclosing class as `$this`'s type or the receiver type, `$this` in scope, a call context — and a method that answers null or differently in a state fails its cell. A cell that fails today is listed with the step that clears it and runs before it skips; a listed cell that passes fails the test (`getNameContext` and `getVariablesInScope` step-36; `resolveAtPosition` step-39; member access and call context pass in every state today). An unregistered method fails. Done: the grid runs in `composer test`; each state's precondition is pinned; every skip names a step; `AstTextAgreementTest` is unchanged.
- [ ] **step-34** — `ParserService::parse()` returns `array<Stmt>`, never null. Delete the five `Parser returned null` throws in `SymbolResolver` and the two `?? []` in `DocumentSymbolSink` and `DocumentIndexer`. `parseFile()` keeps its null for an unreadable path. Done: `parse()` has a non-nullable return type; no caller of `parse()` tests its result against null or defaults it; `composer test` is green.
- [ ] **step-35** — The syntax producer seam. `Parser\SyntaxProducer` has one method, `produce(TextDocument): array<Stmt>`. `Parser\PhpParserProducer` wraps the php-parser call with its error-collecting handler and is the only class that names `PhpParser\Parser`. `ParserService` becomes the composite: it takes an ordered list of producers, returns the first non-empty result (empty when every producer is empty), then runs the parent-connecting and name-resolving traversal and the content-keyed memo and metrics exactly as today. `Server::forProject` assembles the list with the one producer. The tree shape is php-parser's node model; a future producer for another parser converts into it, and nothing above the composite learns which producer answered. This row names the Lateral edit: the `PhpParser\Parser*`, `ParserFactory`, and `Lexer*` allowlist path in `phpstan.neon` moves from `ParserService` to `PhpParserProducer`; the traversal and filesystem paths stay on `ParserService`. Done: behaviour-preserving (every parity golden unchanged); `SyntaxProducer` has one implementation; the ledger's syntax-tree row is unskipped and green; `CLAUDE.md` Project Structure describes the composite.
- [ ] **step-36** — The skeleton producer. `Parser\SkeletonProducer` implements `SyntaxProducer` from the structural regexes now in `TextFallbackHelper` (`findClassLikeDeclarations`, `matchMethodsInBody`, `matchPropertiesInBody`, `matchConstantsInBody`, `nameContextFromText` and the namespace and import helpers behind it), which move into it. It builds `Namespace_` (with the `kind` attribute `ScopeFinder::findNamespaceNodeAtLine` reads), `Use_` and `GroupUse`, the four class-like kinds with `extends` and `implements`, and the members exactly as the text producer describes them today and no more: `ClassMethod` with visibility, static-ness, name, no parameters, and an empty body; `Property` with visibility, static-ness, readonly-ness, and name; `ClassConst` with visibility and name. Every node carries `startLine`, `endLine`, `startFilePos`, and `endFilePos`, which php-parser 5 records on every node it produces and which `NodeAtPosition`, `Scope::atOffset`, `VariableBindings`, and `ScopeFinder` read; a method whose body has no closing brace ends at end of file, as `sliceClassBody` already decides. The composite's traversal adds `parent`, `resolvedName`, and `namespacedName` to a skeleton tree as it does to a parsed one, so `Scope::atOffset` finds the enclosing method and class and `DeclarationScanner` finds the declarations with no change. `ParserService`'s no-argument constructor registers it after `PhpParserProducer`, so every construction site gets the production list. Two tests pin the old behaviour and are updated by this row, not deleted: `CompletionHandlerTest::testCompletionThisInVeryBrokenFile`'s precondition asserts that php-parser alone yields nothing (as the grid's precondition does) rather than that `ParserService` does; `DocumentSymbolSinkTest`'s preserved-registration and text-extractor cases construct the parser with the php-parser producer alone, since the tiers they test are unreachable with the skeleton present (step-37 removes them). `AstTextAgreementTest` gains producer agreement: on every fixture that parses, a skeleton tree and a parsed tree yield the same namespace, imports, class-like names and kinds, and the same member names, visibility, static-ness, and readonly-ness through `DeclarationScanner` and `DefaultClassInfoFactory`, and the same enclosing class-like at every line the enclosing-class section uses. This row names the Lateral edit: the regex allowlist path moves from `TextFallbackHelper` to `SkeletonProducer` for the moved methods. Done: the moved methods no longer exist on `TextFallbackHelper`; the grid's `getNameContext` and `getVariablesInScope` cells pass in the empty state and their entries are gone from `EMPTY_STATE_SKIPS`; the broken-file completion parity goldens are byte-for-byte unchanged; producer agreement runs on every fixture the enclosing-class and name-context agreement sections use and on every fixture under `tests/Fixtures/src/Domain/`.
- [ ] **step-37** — One write path. `DocumentSymbolSink::write` parses, scans, registers, indexes — no tier selection. Delete `TextSymbolExtractor`, `DefaultTextSymbolExtractor`, `NullTextSymbolExtractor`, `OpenDocumentBackend::hasRegistrationFor`, and the `ClassInfoFactory` text case step-16 added; the skeleton tree feeds `DeclarationScanner` and `DeclarationSymbolInfoFactory` like any tree. `DocumentSymbolSinkTest` pins one path. Done: the sink's constructor names no text class; the ledger's cursor-syntax skip for `DefaultTextSymbolExtractor` is gone (the class is gone); the broken-file completion parity goldens are byte-for-byte unchanged; `WritePathParityTest` is green.
- [ ] **step-38** — The cursor seam. `Resolution\CursorNode::at(array $ast, TextDocument, int $offset): ?Node` returns the tree's node at the offset through `NodeAtPosition` and nothing else yet. `SymbolResolver::resolveAtPosition`, `MemberAccessDetector`, and `CallContextDetector` obtain the node at the cursor through it. Behaviour-preserving. Done: no class in `src/Resolution/` other than `CursorNode` names `NodeAtPosition`; every parity golden is unchanged.
- [ ] **step-39** — The cursor reader answers member access. When the tree has no node at the offset, `CursorNode::at` builds one from the text before the cursor: `matchMemberAccessAt` moves from `TextFallbackHelper` into `CursorNode`, and its match becomes a `PropertyFetch` or `StaticPropertyFetch` whose receiver is a plain `Variable` or `Name` node and whose name is php-parser's `Expr\Error` when nothing follows the arrow — the shape php-parser's own recovery produces and `MemberAccessDetector::detect` already accepts. The synthesized nodes carry `startFilePos`, `endFilePos`, and `startLine` from the match offsets, so the tree's enclosing class and name context resolve the receiver with no side-channel attribute; `EnclosingClassResolver::seedThisPosition` has no caller and is deleted. Delete `MemberAccessDetector::fromText`. `AstTextAgreementTest`'s member-access section becomes cursor agreement: on every fixture that parses, `CursorNode::at` with the tree and with an empty tree returns nodes of the same class with the same receiver and name. This row names the Lateral edit: the regex allowlist path moves from `TextFallbackHelper` to `CursorNode` for the moved method. Done: `MemberAccessDetector` names neither `TextFallbackHelper` nor `EnclosingClassResolver`; the grid's `resolveAtPosition` cell passes in the empty state and its entry is gone from `EMPTY_STATE_SKIPS`; the grid's `getMemberAccessContext` cell still passes in every state; the ledger's cursor-syntax skip for `MemberAccessDetector` is gone; the broken-file completion parity goldens are byte-for-byte unchanged.
- [ ] **step-40** — The cursor reader answers calls. `detectCallFromText`, `parseCallPattern`, `parseArgsFromText`, and `findUnclosedParen` move from `TextFallbackHelper` into `CursorNode`, and their match becomes the `FuncCall`, `MethodCall`, `StaticCall`, or `New_` node the text path already builds today, positioned as in step-39. Delete `CallContextDetector::fromText` and the AST-then-text-then-AST chain in `SymbolResolver::getCallContext`: one `CursorNode::at`, one detection, one resolution. `AstTextAgreementTest`'s call-context section becomes cursor agreement in the same form as step-39's. This row names the Lateral edit: the regex allowlist path moves for the moved methods. Done: `CallContextDetector` does not name `TextFallbackHelper`; `getCallContext` calls `CursorNode::at` once and `resolveCallable` once; the grid's `getCallContext` cell still passes in every state; the ledger's cursor-syntax skip for `CallContextDetector` is gone; the signature-help parity goldens are byte-for-byte unchanged.
- [ ] **step-41** — Delete `TextFallbackHelper` and `EnclosingClassResolver`. What remains on the helper after steps 36, 39, and 40 (`findEnclosingClass`, `findEnclosingClassFromContent`, `findNamespace`, `matchParameterType`, `resolveEnclosingClassName`, `resolvedName`) has no caller once the tree always carries the enclosing class and namespace: `Scope::atOffset` answers the enclosing class-like and `NameContextFactory::fromAst` the name context, on every tree. Delete `NameContextFactory::fromAstOrText` and `fromText`, `EnclosingClassResolver` whole, and `ScopeFinder::findEnclosingClassName` and `findEnclosingClassNode` where `Scope` already answers. This row names the Tighten: remove `TextFallbackHelper::resolveEnclosingClassName` from `disallowedMethodCalls` and remove the helper's path from the regex allowlist (both delete an entry for a thing that no longer exists), and remove the ledger's cursor-syntax row with the class it watched. Done: `src/Resolution/TextFallbackHelper.php` and `EnclosingClassResolver.php` are gone; `NameContextFactory` has one public constructor method; `SymbolResolver`'s constructor builds no text helper; the ledger has no cursor-syntax row and no skip that names step-41; `CLAUDE.md` no longer says incomplete code is handled via `TextFallbackHelper`.
- [ ] **step-42** — `SymbolExtractor` reads `DeclarationScanner` and each declaration's `namespacedName`, and walks nothing itself; its hand-tracked namespace is gone. Done: `SymbolExtractor` has no `NodeTraverser` or `NodeVisitorAbstract` and no namespace string assembly; `CLAUDE.md` no longer names it as the surviving traversal; `assertStoresAgree` still passes on every fixture.
- [ ] **step-43** — `Resolution\NameContext` wraps php-parser's engine. `NameContextFactory::fromAst` feeds the tree's `Namespace_`, `Use_`, and `GroupUse` nodes into a `PhpParser\NameContext` through `startNamespace` and `addAlias`, and `candidates(short, NameKind)` becomes a thin adapter: `getResolvedClassName` for class-likes; for functions and constants `getResolvedName`, which returns null for an unqualified name, in which case the adapter yields the namespaced spelling then the global one, as PHP does. Behaviour-preserving: `candidates()` answers exactly what it answers today. Done: `Resolution\NameContext` holds no import arrays of its own; a test asserts `candidates()` on every existing name-context fixture before and after is identical; `AstTextAgreementTest`'s name-context section is green through the adapter; the ledger's name-resolution row names `Resolution\NameContext` as holder and is green.
- [ ] **step-44** — Every other route to a full name reads the parser's `resolvedName` or `namespacedName` attribute and nothing else: `ScopeFinder::resolveClassName`, `DefaultClassInfoFactory::resolveClassName` and `resolveNameToClassName`, `ExpressionResolver::resolveShortClassName`. Any hand-written import table, alias map, or namespace concatenation in `src/` is a finding. Done: `src/` contains no string concatenation that builds a fully qualified name from a namespace and a short name outside `Domain\NamespacePath` and `QualifiedName`; hover, definition, and completion on an aliased import, a group import, and a namespaced constant with global fallback answer as before.
- [ ] **step-45** — One open-document store. `OpenDocumentBackend` answers `lookup`, `childrenOf`, and `search` from one map of `DeclaredSymbol`s per document, built by `DeclarationSymbolInfoFactory`. Delete `SymbolIndex`, `DocumentIndexer`, `SymbolExtractor`, `Index\Symbol`, `WorkspaceNamespaceSource`, and `DocumentSymbolSink::assertStoresAgree` (there are no longer two stores to compare); the tests that pinned two stores agreeing are replaced by tests of the one store (this row authorises that test change). `KnowledgeStack::forProject` no longer takes an index. Done: `src/Index/SymbolIndex.php` is gone; the ledger's open-document row has no skip and names `OpenDocumentBackend` alone; `ChildrenOfParityTest`, `PrefixSearchParityTest`, and `ClassLikeLookupParityTest` goldens are unchanged; `CLAUDE.md` Symbol Backends describes one store.
- [ ] **step-46** — `Type::valueType(): ?Type`. Every `Type` implementation answers it: `ClassName` and `PrimitiveType` from their existing `typeArguments` (the first argument, or null when there is none); `UnionType` and `IntersectionType` from their members when every member agrees and null otherwise; `LateStaticType` null. `TypeFactory::fromNode` and `fromReflection` do not populate it yet. Scaffolding for step-47. Done: every `Type` implementation has the method; a test per implementation pins the answer; `TypeConstructionRule` and `ConfinementCoverageTest` still pass.
- [ ] **step-47** — One type per member. `TypeFactory::fromDocblock(string $docblock, NameContext): ?Type` is the only reader of docblock type tags (`@var`, `@return`, `@param`, and the `psalm-`/`phpstan-` spellings the parser already knows), and `TypeFactory::merge(?Type $native, ?Type $docblock): ?Type` the only place the two are reconciled: native wins on conflict, and a docblock supplies the value type for a native `array`, `iterable`, or class type through `typeArguments`. Both `ClassInfo` factories and `FunctionInfo` build `type`, `returnType`, and parameter types through `merge`. `DocblockParser::arrayElementType` moves behind `fromDocblock`. Done: `TypeFactory` is the only `src/` class that names `DocblockParser` for a type (the presenter still names it for descriptions); a test asserts that a `@return list<User>` method's `returnType->valueType()` is `User`; `FunctionSurfaceParityTest` goldens are unchanged.
- [ ] **step-48** — `ExpressionResolver::foreachElementType` reads `valueType()` on the resolved expression's type and never a docblock. This row names the Tighten: `disallowedMethodCalls` restricts `ResolvedSymbol::getDocumentation()` to `ResolvedSymbolPresenter` alone (the `ExpressionResolver` allowlist path from step-25 is removed). Done: `ExpressionResolver` names neither `DocblockParser` nor `getDocumentation()`; `foreach` over a `@return list<User>` method still resolves members; the ledger's docblock-types row has no skip.
- [ ] **step-49** — One built-in origin predicate. `Knowledge\BuiltinOrigin::isTarget(ReflectionClass|ReflectionFunction|ReflectionFunctionAbstract): bool` is the one place that asks whether a reflected symbol belongs to the target environment rather than the server's own runtime (today `isInternal()`, and the one place to change when RFC 1 §4.7 gains a target-environment parameter). `ReflectionSymbolInfoFactory` and `ReflectionNamespaceSource` call it. This row names the Tighten: `disallowedMethodCalls` restricts `ReflectionClass::isInternal()`, `ReflectionFunction::isInternal()`, and `ReflectionFunctionAbstract::isInternal()` to `src/Knowledge/BuiltinOrigin.php`; the reflection allowlist gains that one file and that gain is authorised here. Done: `isInternal()` is called in one `src/` file; hover on a class the server loads but PHP does not ship still answers nothing (#429's test stays green); the built-in parity goldens are unchanged.
- [ ] **step-50** — Retire the rebuild. Delete this manifest, the `do-next` and `review-slice` skills (this row authorises the `.claude/` and policy deletions), and the manifest read from `SymbolCoverageGridTest` so a blocker must name an issue or an RFC section. `OneRoutePerFactTest` and `ParseHealthGridTest` stay: they are the mechanism, and a new fact or a new positional method is a new row in one of them. Restore the hard lock — `bin/check-baseline-shrink` fails if either baseline file exists (this reverses the shrink-only-when-present relaxation carried since PR #479). Add a `tests/Architecture/HandlerDependenciesRule.php` (or an equivalent architecture test) that fails if any handler in `src/Handler/` names `ParserService`, `MemberResolver`, or `SymbolSource` directly — the "handlers are formatters, not resolvers" invariant becomes code-enforced. Update `CLAUDE.md` Guardrails and Architecture Invariants to match final state: baselines permanently gone, the closed-rule-set paragraph deleted, the running list of active `disallowedMethodCalls` tightens (from #478 and steps 27-31, 41, 48, 49) named as the seams they pin. Done: this file and both skills are gone; both baseline files are absent; `bin/check-baseline-shrink` fails with them present; the handler-dependencies rule is in place and green; neither ledger has a skipped row; `CLAUDE.md` matches reality; `composer test` is green; work continues as plain issues.
